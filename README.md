# Loopback firsat API

First close up to Loopback, trying with a simple products API.

Create controller:

```
$| lb4 controller
```

Crerate datasource:

```
$| lb4 datasource
```

Create model:

```
$| lb4 model
```

Create repository:

``` 
$| lb4 repository
```
